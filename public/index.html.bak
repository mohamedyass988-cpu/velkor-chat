<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>Velkor</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start; /* ⬅️ رفع المحتوى */
      background: linear-gradient(180deg, #eef3fb, #f7f9fd);
      font-family: system-ui, -apple-system, BlinkMacSystemFont;
    }

    .wrapper {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 18px;              /* ⬅️ مثل أول مرة */
      margin-top: 40px;       /* ⬅️ تحكم بالارتفاع */
    }

    /* اللوجو */
    .logo {
      width: 130px;           /* ⬅️ أكبر فقط */
      height: 130px;
      object-fit: contain;
    }

    /* الكرت (رجع مثل الأول) */
    .card {
      background: #fff;
      width: 100%;
      max-width: 360px;
      padding: 32px 26px 34px; /* ⬅️ نفس القيم القديمة */
      border-radius: 16px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.08);
      text-align: center;
    }

    .card h1 {
      margin: 0 0 22px;
      font-size: 22px;
      font-weight: 700;
    }

      .card input {
  width: 100%;
  padding: 12px;          /* ⬅️ عادي مثل أي موقع */
  margin-bottom: 12px;
  border-radius: 8px;
  border: 1px solid #ddd;
  font-size: 15px;
  outline: none;
  box-sizing: border-box;
}

    .card button {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 10px;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
    }

    .login {
      background: #1f6feb;
      color: #fff;
      margin-bottom: 12px;
    }

    .register {
      background: #2ea44f;
      color: #fff;
    }
  </style>
</head>
<body>

  <div class="wrapper">

    <img src="logo.png" alt="Velkor Logo" class="logo">

    <div class="card">
      <h1>Velkor</h1>

      <input id="username" type="text" placeholder="اسم المستخدم">
      <input id="password" type="password" placeholder="كلمة المرور">

      <button class="login" onclick="login()">تسجيل الدخول</button>
      <button class="register" onclick="goRegister()">إنشاء حساب</button>
    </div>

  </div>

<script>
function goRegister() {
  window.location.href = "register.html";
}

function login() {
  fetch("/login", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      username: document.getElementById("username").value,
      password: document.getElementById("password").value
    })
  })
  .then(r => r.json())
  .then(d => {
    if (d.ok) {
      window.location.href = "/chat.html";
    } else {
      alert(d.error);
    }
  });
}
</script>

</body>
</html>